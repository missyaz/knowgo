server:
  port: 9000

spring:
  application:
    name: nfturbo-order
  config:
    import:
      - classpath:base.yml
      - classpath:rpc.yml
#      - classpath:datasource-sharding.yml
      - classpath:cache.yml
#      - classpath:job.yml
#      - classpath:stream.yml
#      - classpath:seata.yml
#      - classpath:limiter.yml