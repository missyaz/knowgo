spring:
  application:
    name: @application.name@
  config:
    import:
      - classpath:base.yml
      - classpath:datasource.yml
      - classpath:rpc.yml
      - classpath:cache.yml
#      - classpath:stream.yml
#      - classpath:es.yml
#      - classpath:seata.yml

server:
  port: 8099