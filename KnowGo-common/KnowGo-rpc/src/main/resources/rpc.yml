dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${com.fw.know.go.nacos.server.url}
    parameters:
      namespace: ${{com.fw.know.go.dubbo.nacos.namespace}
      group: ${{com.fw.know.go.dubbo.nacos.group}
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false